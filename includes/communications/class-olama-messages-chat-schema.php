<?php
/** Additive Release B tables. No foreign keys into another plugin's data. */
if ( ! defined( 'ABSPATH' ) ) { exit; }

class Olama_Messages_Chat_Schema {
    public static function definitions() {
        return array(
            'threads' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                thread_key varchar(191) COLLATE utf8mb4_bin NOT NULL,
                kind varchar(20) NOT NULL,
                subject varchar(190) NOT NULL,
                context_json longtext NOT NULL,
                inbox_id bigint unsigned NOT NULL DEFAULT 0,
                requester_key varchar(191) COLLATE utf8mb4_bin NOT NULL DEFAULT '',
                assignee_key varchar(191) COLLATE utf8mb4_bin NOT NULL DEFAULT '',
                status varchar(20) NOT NULL DEFAULT 'open',
                service_cycle int unsigned NOT NULL DEFAULT 1,
                service_cycle_started_at_utc datetime NULL,
                response_escalated_cycle int unsigned NOT NULL DEFAULT 0,
                resolution_escalated_cycle int unsigned NOT NULL DEFAULT 0,
                department_viewed_at_utc datetime NULL,
                department_responded_at_utc datetime NULL,
                resolved_at_utc datetime NULL,
                last_message_id bigint unsigned NOT NULL DEFAULT 0,
                created_at_utc datetime NOT NULL,
                PRIMARY KEY  (id),
                UNIQUE KEY thread_key (thread_key),
                KEY inbox (inbox_id,status,id),
                KEY activity (last_message_id,id)",
            'thread_participants' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                thread_id bigint unsigned NOT NULL,
                actor_key varchar(191) COLLATE utf8mb4_bin NOT NULL,
                display_name varchar(190) NOT NULL,
                delivered_cursor bigint unsigned NOT NULL DEFAULT 0,
                read_cursor bigint unsigned NOT NULL DEFAULT 0,
                unread_count int unsigned NOT NULL DEFAULT 0,
                manual_unread tinyint NOT NULL DEFAULT 0,
                archived tinyint NOT NULL DEFAULT 0,
                muted tinyint NOT NULL DEFAULT 0,
                pinned tinyint NOT NULL DEFAULT 0,
                participated_at_utc datetime NULL,
                PRIMARY KEY  (id),
                UNIQUE KEY participant (thread_id,actor_key),
                KEY actor (actor_key,thread_id)",
            'messages' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                thread_id bigint unsigned NOT NULL,
                sender_key varchar(191) COLLATE utf8mb4_bin NOT NULL,
                authenticated_wp_user_id bigint unsigned NOT NULL,
                display_name varchar(190) NOT NULL,
                client_message_id char(36) COLLATE utf8mb4_bin NOT NULL,
                request_hash char(64) NOT NULL,
                body text NOT NULL,
                reply_to bigint unsigned NOT NULL DEFAULT 0,
                sent_at_utc datetime NOT NULL,
                edited_at_utc datetime NULL,
                redacted_at_utc datetime NULL,
                PRIMARY KEY  (id),
                UNIQUE KEY client_message (sender_key,client_message_id),
                KEY sent (sent_at_utc,id),
                KEY thread (thread_id,id)",
            'message_revisions' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                message_id bigint unsigned NOT NULL,
                body text NOT NULL,
                actor_key varchar(191) COLLATE utf8mb4_bin NOT NULL,
                authenticated_wp_user_id bigint unsigned NOT NULL,
                created_at_utc datetime NOT NULL,
                PRIMARY KEY  (id),
                KEY message (message_id,id)",
            'service_inboxes' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                name varchar(190) NOT NULL,
                active tinyint NOT NULL DEFAULT 1,
                allow_family tinyint NOT NULL DEFAULT 1,
                allow_teacher tinyint NOT NULL DEFAULT 1,
                show_employee tinyint NOT NULL DEFAULT 0,
                history_policy varchar(30) NOT NULL DEFAULT 'active_and_recent',
                recent_days int unsigned NOT NULL DEFAULT 30,
                response_sla_minutes int unsigned NOT NULL DEFAULT 1440,
                resolution_sla_minutes int unsigned NOT NULL DEFAULT 4320,
                created_at_utc datetime NOT NULL,
                PRIMARY KEY  (id)",
            'inbox_members' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                inbox_id bigint unsigned NOT NULL,
                actor_key varchar(191) COLLATE utf8mb4_bin NOT NULL,
                active tinyint NOT NULL DEFAULT 1,
                is_manager tinyint NOT NULL DEFAULT 0,
                PRIMARY KEY  (id),
                UNIQUE KEY membership (inbox_id,actor_key),
                KEY actor (actor_key,active,inbox_id)",
            'thread_actions' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                thread_id bigint unsigned NOT NULL,
                action varchar(30) NOT NULL,
                actor_key varchar(191) COLLATE utf8mb4_bin NOT NULL,
                authenticated_wp_user_id bigint unsigned NOT NULL,
                target_key varchar(191) COLLATE utf8mb4_bin NOT NULL DEFAULT '',
                created_at_utc datetime NOT NULL,
                PRIMARY KEY  (id),
                KEY thread (thread_id,id)",
            'restrictions' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                actor_key varchar(191) COLLATE utf8mb4_bin NOT NULL,
                restriction_type varchar(30) NOT NULL,
                scope_type varchar(30) NOT NULL,
                scope_key varchar(191) COLLATE utf8mb4_bin NOT NULL DEFAULT '',
                target_key varchar(191) COLLATE utf8mb4_bin NOT NULL DEFAULT '',
                private_reason text NOT NULL,
                starts_at_utc datetime NOT NULL,
                ends_at_utc datetime NULL,
                revoked_at_utc datetime NULL,
                PRIMARY KEY  (id),
                KEY actor (actor_key,revoked_at_utc,starts_at_utc)",
            'reports' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                message_id bigint unsigned NOT NULL,
                thread_id bigint unsigned NOT NULL,
                reporter_key varchar(191) COLLATE utf8mb4_bin NOT NULL,
                reason text NOT NULL,
                status varchar(20) NOT NULL DEFAULT 'open',
                resolution_note text NOT NULL,
                created_at_utc datetime NOT NULL,
                updated_at_utc datetime NOT NULL,
                PRIMARY KEY  (id),
                UNIQUE KEY reporter (message_id,reporter_key),
                KEY queue (status,id)",
            'chat_changes' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                thread_id bigint unsigned NOT NULL,
                kind varchar(30) NOT NULL,
                created_at_utc datetime NOT NULL,
                PRIMARY KEY  (id),
                KEY thread (thread_id,id)",
            'chat_clock' => "id bigint unsigned NOT NULL,
                sequence bigint unsigned NOT NULL DEFAULT 0,
                PRIMARY KEY  (id)",
            'chat_rate_limits' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                bucket_key char(64) NOT NULL,
                hits int unsigned NOT NULL DEFAULT 0,
                expires_at_utc datetime NOT NULL,
                PRIMARY KEY  (id),
                UNIQUE KEY bucket (bucket_key),
                KEY expiry (expires_at_utc)"
        );
    }
}
