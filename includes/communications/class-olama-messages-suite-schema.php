<?php
if ( ! defined( 'ABSPATH' ) ) { exit; }

class Olama_Messages_Suite_Schema {
    public static function definitions() {
        return array(
            'attachments' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                uploader_key varchar(191) COLLATE utf8mb4_bin NOT NULL,
                authenticated_wp_user_id bigint unsigned NOT NULL,
                owner_type varchar(30) NOT NULL DEFAULT 'staged',
                owner_id bigint unsigned NOT NULL DEFAULT 0,
                scope_type varchar(30) NOT NULL,
                scope_id bigint unsigned NOT NULL DEFAULT 0,
                original_name varchar(190) NOT NULL,
                extension varchar(12) NOT NULL,
                mime_type varchar(100) NOT NULL,
                size_bytes bigint unsigned NOT NULL,
                sha256 char(64) NOT NULL,
                storage_key varchar(100) NOT NULL,
                variants_json longtext NOT NULL,
                scan_state varchar(20) NOT NULL,
                status varchar(20) NOT NULL,
                active tinyint NOT NULL DEFAULT 1,
                created_at_utc datetime NOT NULL,
                expires_at_utc datetime NULL,
                PRIMARY KEY  (id),
                KEY owner (owner_type,owner_id,active,id),
                KEY expiry (owner_type,status,expires_at_utc,id),
                KEY uploader (uploader_key,id)",
            'action_items' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                action_key char(64) NOT NULL,
                request_hash char(64) NOT NULL DEFAULT '',
                source_type varchar(30) NOT NULL,
                source_id bigint unsigned NOT NULL,
                thread_id bigint unsigned NOT NULL DEFAULT 0,
                owner_key varchar(191) COLLATE utf8mb4_bin NOT NULL DEFAULT '',
                owner_inbox_id bigint unsigned NOT NULL DEFAULT 0,
                title varchar(190) NOT NULL,
                status varchar(20) NOT NULL DEFAULT 'open',
                priority varchar(20) NOT NULL DEFAULT 'normal',
                due_at_utc datetime NULL,
                created_by_key varchar(191) COLLATE utf8mb4_bin NOT NULL,
                created_by_wp_user_id bigint unsigned NOT NULL,
                version int unsigned NOT NULL DEFAULT 1,
                started_at_utc datetime NULL,
                resolved_at_utc datetime NULL,
                resolution_note text NOT NULL,
                created_at_utc datetime NOT NULL,
                updated_at_utc datetime NOT NULL,
                overdue_notified_version int unsigned NOT NULL DEFAULT 0,
                PRIMARY KEY  (id),
                UNIQUE KEY action_key (action_key),
                KEY owner (owner_key,status,due_at_utc,id),
                KEY creator (created_by_key,id),
                KEY inbox (owner_inbox_id,status,id),
                KEY source (source_type,source_id),
                KEY due (status,due_at_utc,id)",
            'action_history' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                action_id bigint unsigned NOT NULL,
                version int unsigned NOT NULL,
                actor_key varchar(191) COLLATE utf8mb4_bin NOT NULL,
                authenticated_wp_user_id bigint unsigned NOT NULL,
                change_json longtext NOT NULL,
                created_at_utc datetime NOT NULL,
                PRIMARY KEY  (id),
                KEY action (action_id,id)",
            'events' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                source_type varchar(40) NOT NULL DEFAULT 'communications',
                source_id varchar(100) COLLATE utf8mb4_bin NOT NULL,
                source_version varchar(100) NOT NULL DEFAULT '',
                source_checked_at_utc datetime NULL,
                source_error varchar(190) NULL,
                title varchar(190) NOT NULL,
                description longtext NOT NULL,
                category varchar(40) NOT NULL,
                priority varchar(20) NOT NULL DEFAULT 'normal',
                starts_at_utc datetime NOT NULL,
                ends_at_utc datetime NOT NULL,
                timezone varchar(80) NOT NULL,
                all_day tinyint NOT NULL DEFAULT 0,
                location varchar(190) NOT NULL DEFAULT '',
                deep_link varchar(500) NOT NULL DEFAULT '',
                status varchar(30) NOT NULL DEFAULT 'draft',
                response_scope varchar(20) NOT NULL DEFAULT 'actor',
                requires_ack tinyint NOT NULL DEFAULT 0,
                rsvp_enabled tinyint NOT NULL DEFAULT 0,
                action_required tinyint NOT NULL DEFAULT 0,
                action_due_at_utc datetime NULL,
                completion_provider varchar(80) NOT NULL DEFAULT '',
                content_version int unsigned NOT NULL DEFAULT 1,
                ack_required_version int unsigned NOT NULL DEFAULT 0,
                rsvp_required_version int unsigned NOT NULL DEFAULT 0,
                reminder_policy_json longtext NOT NULL,
                snapshot_id char(36) NOT NULL,
                published_at_utc datetime NULL,
                created_by_key varchar(191) COLLATE utf8mb4_bin NOT NULL,
                created_by_wp_user_id bigint unsigned NOT NULL,
                created_at_utc datetime NOT NULL,
                updated_at_utc datetime NOT NULL,
                PRIMARY KEY  (id),
                UNIQUE KEY source (source_type,source_id),
                KEY calendar (starts_at_utc,id),
                KEY status (status,id)",
            'event_audiences' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                event_id bigint unsigned NOT NULL,
                snapshot_id char(36) NOT NULL,
                audience_json longtext NOT NULL,
                status varchar(30) NOT NULL DEFAULT 'draft',
                cursor_offset bigint unsigned NOT NULL DEFAULT 0,
                source_versions_json longtext NULL,
                started_at_utc datetime NULL,
                completed_at_utc datetime NULL,
                created_by_key varchar(191) COLLATE utf8mb4_bin NOT NULL,
                PRIMARY KEY  (id),
                UNIQUE KEY snapshot (snapshot_id),
                KEY event (event_id,id)",
            'event_targets' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                event_id bigint unsigned NOT NULL,
                snapshot_id char(36) NOT NULL,
                actor_key varchar(191) COLLATE utf8mb4_bin NOT NULL,
                context_key char(64) NOT NULL,
                student_uid varchar(191) COLLATE utf8mb4_bin NOT NULL DEFAULT '',
                display_name_snapshot varchar(190) NOT NULL,
                context_json longtext NOT NULL,
                reachability varchar(30) NOT NULL DEFAULT 'unchecked',
                released_version int unsigned NOT NULL DEFAULT 0,
                delivered_content_version int unsigned NOT NULL DEFAULT 0,
                read_content_version int unsigned NOT NULL DEFAULT 0,
                acknowledged_ack_version int unsigned NOT NULL DEFAULT 0,
                acknowledged_content_version int unsigned NOT NULL DEFAULT 0,
                rsvp_status varchar(20) NOT NULL DEFAULT 'pending',
                rsvp_content_version int unsigned NOT NULL DEFAULT 0,
                rsvp_required_version int unsigned NOT NULL DEFAULT 0,
                sent_at_utc datetime NULL,
                delivered_at_utc datetime NULL,
                read_at_utc datetime NULL,
                acknowledged_at_utc datetime NULL,
                rsvp_at_utc datetime NULL,
                checked_at_utc datetime NULL,
                PRIMARY KEY  (id),
                UNIQUE KEY target (event_id,actor_key,context_key),
                KEY event_page (event_id,id),
                KEY actor (actor_key,id),
                KEY snapshot (snapshot_id,id)",
            'event_revisions' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                event_id bigint unsigned NOT NULL,
                content_version int unsigned NOT NULL,
                ack_required_version int unsigned NOT NULL,
                rsvp_required_version int unsigned NOT NULL,
                snapshot_json longtext NOT NULL,
                change_policy_json longtext NOT NULL,
                actor_key varchar(191) COLLATE utf8mb4_bin NOT NULL,
                authenticated_wp_user_id bigint unsigned NOT NULL,
                created_at_utc datetime NOT NULL,
                PRIMARY KEY  (id),
                UNIQUE KEY version (event_id,content_version)",
            'event_reminders' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                event_id bigint unsigned NOT NULL,
                content_version int unsigned NOT NULL,
                offset_minutes int unsigned NOT NULL,
                run_at_utc datetime NOT NULL,
                status varchar(20) NOT NULL DEFAULT 'pending',
                PRIMARY KEY  (id),
                UNIQUE KEY slot (event_id,content_version,offset_minutes),
                KEY due (status,run_at_utc,id)",
            'activity_notifications' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                activity_key char(64) NOT NULL,
                actor_key varchar(191) COLLATE utf8mb4_bin NOT NULL,
                object_type varchar(30) NOT NULL,
                object_id bigint unsigned NOT NULL,
                object_version int unsigned NOT NULL DEFAULT 0,
                kind varchar(40) NOT NULL,
                critical tinyint NOT NULL DEFAULT 0,
                created_at_utc datetime NOT NULL,
                delivered_at_utc datetime NULL,
                seen_at_utc datetime NULL,
                PRIMARY KEY  (id),
                UNIQUE KEY activity (activity_key),
                KEY actor (actor_key,id),
                KEY unseen (actor_key,seen_at_utc,id)",
            'communication_preferences' => "id bigint unsigned NOT NULL AUTO_INCREMENT,
                actor_key varchar(191) COLLATE utf8mb4_bin NOT NULL,
                quiet_enabled tinyint NOT NULL DEFAULT 0,
                quiet_start int unsigned NOT NULL DEFAULT 1320,
                quiet_end int unsigned NOT NULL DEFAULT 420,
                previews tinyint NOT NULL DEFAULT 0,
                sound tinyint NOT NULL DEFAULT 0,
                PRIMARY KEY  (id),
                UNIQUE KEY actor (actor_key)"
        );
    }
}
